import { URL } from 'node:url';
import { withoutTrailingSlashes } from "../utils/string.utils.mjs";
import * as defaultNotFoundHandler from "../components/default-not-found.mjs";
export const httpAdapter = {
  getPathname(req) {
    const {
      pathname
    } = new URL(withoutTrailingSlashes(req.url || ''), `https://${req.headers.host}`);
    return pathname;
  },
  getMethod(req) {
    return req.method?.toLowerCase();
  },
  defaultNotFoundHandler: defaultNotFoundHandler.default
};
//# sourceMappingURL=http-adapter.mjs.map