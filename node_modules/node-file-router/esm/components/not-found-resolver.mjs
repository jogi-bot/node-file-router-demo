import { getFileWithExt } from "../utils/file.utils.mjs";
export function resolveNotFoundHandler(basePath) {
  const userHandler = getFileWithExt(basePath, '_404');
  if (userHandler) {
    return import(userHandler).then(file => file.default);
  }
}
//# sourceMappingURL=not-found-resolver.mjs.map