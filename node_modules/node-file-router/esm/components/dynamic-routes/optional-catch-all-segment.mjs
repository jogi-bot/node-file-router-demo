import { createRouteSegmentParamsParser } from "./common/route-params-parser.mjs";
const pattern = /\/\[\[(\.\.\.\w+)]]/g;
export const optionalCatchAllSegment = {
  type: 'optional-catch-all',
  parse: createRouteSegmentParamsParser({
    pattern,
    paramExtractor: value => value.split('/'),
    sanitizeParam: param => param.slice('/[[...'.length, -']]'.length),
    routeParamPattern: '\\/?(?<:key>.*)'
  }),
  isMatch: route => new RegExp(pattern).test(route)
};
//# sourceMappingURL=optional-catch-all-segment.mjs.map